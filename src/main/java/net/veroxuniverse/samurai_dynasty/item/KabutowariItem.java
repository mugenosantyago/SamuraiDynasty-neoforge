package net.veroxuniverse.samurai_dynasty.item;

import net.minecraft.world.item.ToolMaterial;

/**
 * Kabutowari (helmet breaker) weapon. Ported from 1.21.x.
 */
public class KabutowariItem extends ESWeaponItem {

    public KabutowariItem(ToolMaterial tier, float attackDamage, float attackSpeed, Properties properties) {
        super(tier, attackDamage, attackSpeed, properties);
    }
}
